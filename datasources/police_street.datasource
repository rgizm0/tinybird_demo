DESCRIPTION >
    Generated from fixtures/police_street.csv

SCHEMA >
    `crime_id` Nullable(String),
    `month` String,
    `reported_by` String,
    `falls_within` String,
    `longitude` Float32,
    `latitude` Float32,
    `location` String,
    `lsoa_code` String,
    `lsoa_name` String,
    `crime_type` String,
    `last_outcome_category` Nullable(String),
    `context` Nullable(String)